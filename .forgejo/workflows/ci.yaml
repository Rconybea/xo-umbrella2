name: CI
on:
  push:
    branches: [main]

env:
  PATH: /nix/var/nix/profiles/default/bin:$PATH

jobs:
  smoke-test:
    runs-on: host
    steps:
      - name: Check nix version
        run:
          echo $PATH
          #nix --version
